def xorgate(a,b):#defining the function
    xor=a^b# ^is the operator to calculate xor gate
    return xor# returning the value
def andgate(a,b):#defining the function
    and_=a&b# & is the operator to calculate and gates
    return and_#returning the value
def orgate(a,b):# defining the function 
    or_=a|b#| is the operator to calculate or
    return or_#returning the value



